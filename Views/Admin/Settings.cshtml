@{
    ViewData["Title"] = "Settings";
}

<div class="container mt-4">
    <h2>System Settings</h2>
    <p class="text-muted">Configure system settings</p>

    <div class="card mt-4">
        <div class="card-body">
            <h5 class="card-title">This page is only accessible to Admins</h5>
            <p>You have the "ViewSettings" permission.</p>
            <a asp-controller="Dashboard" asp-action="Index" class="btn btn-secondary">Back to Dashboard</a>
        </div>
    </div>
</div>
